#console.log(gon.shifts)
#console.log(gon.timesheet)
#alert("What's up?")
#prompt("How ya doing?")
#values = gon.shifts
#
#log = []
#angular.forEach values, ((value, key) ->
  #@push key + ': ' + value
  #return
#), log
#
