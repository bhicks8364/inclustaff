<nav id='admin-nav' class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_url, id: "nav-logo", class: "navbar-brand" do %>
			       <%= @current_agency.name %>
			 <% end %>
    </div>
    
	
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
		<ul class="nav navbar-nav">
			<li><%= link_to @current_company_admin.name, @current_company_admin %></li>
			<li class="dropdown">
			  <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= @current_company_admin.role %> Toolbox <span class="caret"></span></a>
			  <ul class="dropdown-menu">
			    <%= render "company/shared/owner_links" if current_company_admin.owner? %>
			    <%= render "company/shared/manager_links" if current_company_admin.manager? %>
			    <li role="separator" class="divider"></li>
			    <li><%= link_to @current_company_admin.name, @current_company_admin %></li>
			    <li><%= link_to "<i class='fa fa-power-off'></i> Sign Out".html_safe, destroy_company_admin_session_path, method: :delete  %></li>
			  </ul>
			</li>
		</ul>
		<ul class="nav navbar-nav">
			<%= render 'company/shared/clocked_in' if  @current.jobs.on_shift.any? %>
			<%= render 'company/shared/on_break' if @current.jobs.on_break.any? %>
		</ul>
     
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>


