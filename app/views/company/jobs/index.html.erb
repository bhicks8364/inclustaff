<div class='col-md-12'>
  <%= link_to "Archived", archived_company_jobs_path %>
  <% if @jobs %>
  <h1>Active Jobs <small><%= @company.name %></small></h1>
  <div id="" class="table-responsive">
    <table id="" class="table table-bordered">
    <thead>
      <tr>
     
        <th>Status</th>
        <th>Employee</th>
        <th>Title</th>
        <th>Start date</th>
        <th>Pay rate</th>
        <th>Hours this week</th>
        <th>Pay this week</th>
        <th>Total Hours</th>

      </tr>
    </thead>
  
    <tbody>
      <%= content_tag_for(:tr, @jobs.active) do |job| %>
      <% if job.on_shift? %>
          <tr class="bg-success">
        <% else %>
          <tr class="bg-info">
        <% end %>
          <td><%= link_to job.status, company_job_path(job), class: "black button" %></td>
          <td><%= link_to job.employee.name, company_job_path(job), class: "black button bold" %></td>
          <td><%= link_to job.title, company_job_path(job), class: "black button" %></td>
          <td><%= job.start_date.strftime("%B %e, %Y") %></td>
          <td><%= number_to_currency(job.pay_rate) %></td>
          <td><%= job.current_week_hours %></td>
          <td><%= number_to_currency(job.current_week_pay) %></td>
          <td><%= job.total_hours.round(2) %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  
  </div>
  
  <% else %>
  <h3>No jobs to show.</h3>
  <% end %>
</div>



