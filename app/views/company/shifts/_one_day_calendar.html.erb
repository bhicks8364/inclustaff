<div class='lightbox scroll300'>
    <%= calendar events: shifts, attribute: :time_in, number_of_days: 1 do |date, shifts| %>
      <%= date %>
        <br>
      <% shifts.each do |shift| %>
          <%= shift_collaspe(shift) %><br>
           <div class='collapse white' id="collapseShift_<%=shift.id%>">
              <%=link_to shift.employee.name, company_job_path(shift.job), class: "white" %><br>
               <small><%= shift_times(shift) %></small><br>
               <small><%= link_to "#{shift.hours_worked.round(2)} hours", company_shift_path(shift), class: "white" %></small>
            </div>
      <% end %>
    <% end %>
</div>