<nav class="navbar navbar-default navbar-fixed-top ">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_url, id: "nav-logo", class: "navbar-brand" do %>
			       <%= @current_agency.try(:name) || "IncluStaff"%>
			 <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Sign In", sign_in_path if !signed_in? %></li>
        <li><%= link_to "Sign Up", new_user_registration_path if !signed_in? %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "Sign In", sign_in_path if !signed_in? %></li>
            <li><%= link_to "About", about_path if !signed_in? %></li>
            <li><%= link_to "Contact", about_path if !signed_in? %></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to @current_agency.name, @current_agency if @current_agency.present? %></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">One more separated link</a></li>
          </ul>
        </li>
      </ul>
      
     
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>