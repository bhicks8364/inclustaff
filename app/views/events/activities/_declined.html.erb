<%= div_for event do %>
	<i class="fa fa-times fa-fw"></i>
	<%= event.admin.try(:first_name) if !current_page?('/admins/:id') %>
	<%= event.company_admin.try(:first_name) if !current_page?('/company_admins/:id') %>
	
    <strong>	<%= event.action.humanize.downcase %></strong> placing
    <%=  signed_in_link_to event.eventable.employee.first_name, event.eventable %> at <%= event.eventable.company.name %>
		<span class='label label-warning black pull-right'>
		    <%= event.created_at.stamp('11/13 at 8:30am') %>
		</span>

<% end %>