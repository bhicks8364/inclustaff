
<div class='col-md-12 lightbox'>
    <h1><%= @company_admin.name %></h1>
    
   
    <div class='col-md-2'>
        <% if @company_admin.online? %> <%= image_tag("user-green.png", class: 'online-icon') %> <% end %>
        <%= signed_in_link_to( @company_admin.company.name, @company_admin.company) if admin_signed_in? %> 
        <%= @company_admin.role %>

        <%= @company_admin.email %>
        <%= @company_admin.last_sign_in_ip if admin_signed_in? %>
        <br>
        <%= @company_admin.last_sign_in_at.present? ? @company_admin.last_sign_in_at.stamp("11/12 at 11:15am") : "Yet to sign in" %>

    <%= link_to "Edit", edit_admin_company_admin_path(@company_admin), class: "btn btn-primary" %>

        
    </div>
     <div class='col-md-10'>
         <%= render "quicklook", company: @company %>
     </div>
      
</div>