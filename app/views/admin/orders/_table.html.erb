<% if orders.any? %>
<div class="table-responsive">
  <table id="" class="table table-bordered">
    <thead>
      <tr>
        <td>Industry</td>
        <th>Title</th>
        <th>Pay Range</th>
        <th>Open</th>
        <th>Make A Placement</th>
        
      </tr>
    </thead>
  
    <tbody>
      <% orders.each do |order| %>
        <tr>
          <td><%= link_to "<i class='fa fa-pencil fa-lg'></i>".html_safe, edit_admin_order_path(order) %> <%= order.industry %></td>
          <td><%= status_tag(order) %> &nbsp;<%= link_to title_for(order), admin_order_path(order) %></td>
          <td><%= pay_range(order) %></td>
          <td><%= order.open_jobs %></td>
          <td class='text-center'><%= link_to "<i class='fa fa-user-plus fa-lg'></i>".html_safe, new_admin_order_job_path(order) %></td>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<% end %>
