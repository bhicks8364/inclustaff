<div class="main-box clearfix">
	<div class="alert alert-danger fade in">
		<button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
		<i class="fa fa-exclamation-triangle fa-fw fa-lg"></i>
		<% if @company.nil? %>
		<strong>You have <%= pluralize(@current.orders.needs_attention.count, 'open job order') %>. </strong>   </div>
		<% else %>
		<strong><%= @company.name %> has  <%= pluralize(@company.orders.needs_attention.count, 'open job order') %>. </strong>   </div>
		<% end %>
	<%= render 'open_table' %>
		
</div>
<div class='main-box clearfix'>
	<h2>All Active Job Orders</h2>
  <%= render 'table' %>
</div>


<div class="col-lg-6">
	<div class="main-box clearfix">
	    <% if @company.present? && @order.present? %>
		    <%= render 'form' %>
	    <% end %>
	</div>
</div>

<div class="col-lg-6">
	<div class="main-box clearfix">
		
		
	</div>
</div>