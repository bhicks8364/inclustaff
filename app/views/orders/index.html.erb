

<table id="orders" class="table table-bordered" cellspacing="0" width="85%%">
  <thead>
    <tr>
      <th>Company</th>
      <th>Title</th>
      <th>Pay Range</th>
      <th>Open </th>
      <th>Urgent</th>
      <th>Start Date</th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.active.each do |order| %>
      <tr>
        <td><%= link_to order.company_name, order.company %></td>
        <td><%= link_to order.title, order %></td>
        <td><%= order.pay_range %></td>
        <td><%= order.number_needed - order.jobs.active.count %></td>
        <td><%= order.urgent %></td>
        <td><%= order.needed_by.strftime("%B %e, %Y") %></td>
        <td><%= order.urgent %></td>
        <td><%= link_to "<span class='glyphicon glyphicon-pencil' aria-hidden='true'></span>".html_safe, edit_company_order_path(order.company, order), class: "btn btn-default" %></td>
        <td><%= link_to "<span class='glyphicon glyphicon-trash' aria-hidden='true'></span>".html_safe, company_order_path(order.company, order), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Order', new_order_path, class: "btn btn-default" %>
