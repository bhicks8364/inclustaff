<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_url, class: 'logo' do %>
				<%= image_tag("logo.png", class: 'logo-img') %>
			 <% end %>
    </div>
    


    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
		<ul class="nav navbar-nav navbar-right">
			<li><%= link_to employee_profile_path do %>
            <%= gravatar_for(@current_user, size: 25) %> <%= @current_user.name %>
            <% end %>
        </li>
  			<li><%= link_to "<i class='fa fa-power-off fa-lg' data-placement='bottom' data-toggle='tooltip' title='Sign Out'></i>".html_safe, destroy_user_session_path, method: :delete  %></li>
			<li> <%= link_to "<i class='fa fa-toolbox fa-lg'></i>".html_safe, employee_job_path(@current_user.current_job) if @current_user.current_job.present? %></li>
			<% if current_user.mailbox.inbox.unread(current_user).any? %>
        <li><%= link_to inbox_user_conversations_path do %><i class="fa fa-comments-o"></i> Messages <span class="badge badge-default"><%= current_user.mailbox.inbox.unread(current_user).count %></span><% end %></li>
        <% end %>
		</ul>
		

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>


