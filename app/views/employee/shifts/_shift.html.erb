<li>
     <p><%= link_to shift.job.title, employee_job_path(shift.job) %>
   <%= shift.state.humanize.downcase %>
    <span class="date"><%= time_ago_in_words(shift.updated_at) %></span>
    </p>
</li>